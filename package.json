{
  "name": "game-site",
  "version": "1.0.0",
  "description": "Game site with Vite support",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview",
    "deploy": "npm run build && xcopy dist\\* E:\\game-site\\ /E /I /H /Y"
  },
  "devDependencies": {
    "vite": "^5.2.0"
  },
  "dependencies": {
    "three": "^0.183.2"
  }
}