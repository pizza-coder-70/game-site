document.addEventListener('DOMContentLoaded', () => {
    // Game cards already have onclick attributes in index.html
    // This script can be used for future interactive features.
    console.log('The Pizza Edition - Loaded');
});

